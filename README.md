## DR SAMSON OLORI – Next.js Clone

This is a Next.js 15 App Router + TypeScript + Tailwind CSS replica of the live site `drsamsonolori.com`.

### Running locally

```bash
npm install
npm run dev
```

Then open `http://localhost:3000`. For faster dev server startup, use `npm run dev:turbo` (Turbopack).

